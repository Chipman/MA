(function getAlbums(){$.getJSON('json/albums.json', function (data){var div=''; $.each(data, function (index, el){div +='<div class="album-wrapper">' + '<h2 class="album-header">' + el.title + '</h2>' + '<span class="year">' + el.year + '</span>' + '<img src="' + el.coverm + '" class="album-cover">' + '<p class="album-description">' + el.description + '</p>' + '<a href="' + el.file + '" class="download-button">Cкачати альбом</a>' + '</div>';}); $('<main>',{class: 'posts', html: div}).insertAfter('.conc'); nolinkAlert();});})();