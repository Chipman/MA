(function getAlbums(){$.getJSON('json/concerts.json', function (data){var div=''; $.each(data, function (index, el){div +='<section class="single-date">' + '<a href="' + el.link + '"  title="«устр≥ч ¬контакте" target="_blank"><img src="' + el.image + '" alt="' + el.title + '" class="single-date-img"></a>' + '<span class="concert-date">' + el.date + '</span>' + '<h2 class="single-date-h2"><a href="' + el.link + '" title="«устр≥ч ¬контакте"  target="_blank">&#171;' + el.title + '&#187;</a></h2>' + '<span class="city">м. ' + el.city + '</span>' + '<p class="single-date-p">' + el.description + '</p>' + '</section>';}); $('<main>',{class: 'posts', html: div}).insertAfter('.conc'); nolinkAlert();});})();